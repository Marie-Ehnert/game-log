[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "game-log"
version = "0.1.0"
dependencies = [
    "click",
]

[project.scripts]
game-log = "game_log.log:start_log"

[tool.setuptools.packages.find]
where = ["."]
include = ["game_log*"]